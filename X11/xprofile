#?install ~/.xprofile
#
# xprofile - Set up the environment for X sessions (for logins from xdm)
#

cd ~

# Use .bash_profile (FIXME: just rename it to .profile?)
[ -r ~/.bash_profile ] && . ~/.bash_profile
